# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
OWNER_ID=your_telegram_user_id_here

# Optional: Pyrogram API credentials (can work without these for bot mode)
API_ID=your_api_id
API_HASH=your_api_hash

# Website API Configuration (optional - will use mock API if not provided)
WEBSITE_API_URL=https://your-site.example/api/solve
WEBSITE_API_KEY=your_api_key_or_leave_blank

# Example cURL for real API:
# curl -X POST "${WEBSITE_API_URL}" \
#   -H "Authorization: Bearer ${WEBSITE_API_KEY}" \
#   -H "Content-Type: application/json" \
#   -d '{"q": "What is photosynthesis?", "uid": 123456, "mode": "short"}'
